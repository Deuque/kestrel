{
  "name": "kestrel",
  "version": "0.0.1",
  "private": true,
  "description": "Generic CI test runner: feed it an APK plus an Appium or other integration test suite, get back a pass/fail report with embedded screenshots as a CI artifact.",
  "type": "module",
  "scripts": {
    "run": "tsx src/cli.ts run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
